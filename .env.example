# Copy to .env and fill in. .env is gitignored.

# OpenRouter API key — used by the backend to call the LLM (via LiteLLM),
# routed to the Cerebras inference provider. Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=
